{
  "name": "ultra-image",
  "version": "0.0.1",
  "scripts": {
    "start": "bundle exec jekyll serve --drafts --incremental",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "UNLICENSED"
}
